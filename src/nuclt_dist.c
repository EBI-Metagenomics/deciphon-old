#include "dcp/nuclt_dist.h"
#include "dcp/cmp.h"

enum dcp_rc dcp_nuclt_dist_write(struct dcp_nuclt_dist const *ndist,
                                 struct dcp_cmp *ctx)
{
    return DCP_SUCCESS;
}
