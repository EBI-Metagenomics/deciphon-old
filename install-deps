#!/bin/bash

export DEBUG=1

bash -c "$(curl -fsSL https://git.io/Jz7Oa)" -s horta/hope || exit 1
bash -c "$(curl -fsSL https://git.io/Jz7Oa)" -s horta/logaddexp || exit 1
bash -c "$(curl -fsSL https://git.io/Jz7Oa)" -s horta/elapsed || exit 1
bash -c "$(curl -fsSL https://git.io/Jz7Oa)" -s horta/almosthere || exit 1
bash -c "$(curl -fsSL https://git.io/Jz7Oa)" -s EBI-Metagenomics/c-containers || exit 1
bash -c "$(curl -fsSL https://git.io/Jz7Oa)" -s EBI-Metagenomics/log || exit 1
bash -c "$(curl -fsSL https://git.io/Jz7Oa)" -s EBI-Metagenomics/hmmer-reader || exit 1
bash -c "$(curl -fsSL https://git.io/Jz7Oa)" -s EBI-Metagenomics/fasta || exit 1
bash -c "$(curl -fsSL https://git.io/Jz7Oa)" -s EBI-Metagenomics/imm || exit 1
